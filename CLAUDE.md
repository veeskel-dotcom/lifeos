# LifeOS — Правила проекта

## Стек
- React 19 + Vite 6 + Tailwind CSS 4 + Dexie (IndexedDB) + Recharts
- PWA, offline-first, без бэкенда
- Язык интерфейса: русский

## Дизайн-прототипы (источник истины)
Прототипы — 62 экрана в 8 файлах (из files (17).zip):
- `w14-core.jsx` — Дашборд, PIN, Онбординг, Quick Add, AI-чат (5)
- `w14-finance.jsx` — Финансы: хаб, расходы, доходы, счета, кредиты, бюджет, подписки, ЖКХ, переводы (14)
- `w14-tasks.jsx` — Задачи, детали задачи, Календарь, Событие (4)
- `w14-invest.jsx` — Портфель, акция, дивиденды, сделки (4)
- `w14-nutrition.jsx` — Дневник, добавление еды, поиск, штрихкод, вода, покупки, приём пищи, AI-калории (8)
- `w14-sport.jsx` — Спорт: хаб, тренировка, история, шаблоны, прогресс, упражнение, итоги, вес, обмеры, AI-тренер, видеоанализ (11)
- `w14-health.jsx` — Здоровье: хаб, сон, рутины (3)
- `w14-settings.jsx` — Ещё, заметки, документы, настройки, профиль, безопасность, AI, экспорт, аналитика, уведомления (13)

Прототипы находятся в `/tmp/files17/` (извлечены из `C:/Users/veesk/Downloads/files (17).zip`).

## Правила работы с кодом

### Прототипы = направление, не слепая замена
- Прототипы показывают целевой дизайн и UX-улучшения
- Но в них могли забыть или упростить существующую логику/фичи
- Прототипы — standalone JSX с инлайн-стилями, проект использует Tailwind

### Не затирай старый код слепо
- Перед изменением — анализируй что потеряется, что улучшится
- Текущий код может содержать логику, которой нет в прототипах
- Если видишь конфликт между прототипом и реализацией — спроси, не затирай

### Порядок сверки
1. Прочитать прототип соответствующего экрана
2. Прочитать текущую реализацию в проекте
3. Сравнить: что нового в прототипе, что есть в коде но нет в прототипе
4. Предложить изменения с пояснением что добавляется / что сохраняется

## Дизайн-система
- Стиль: Apple Health + Тинькофф минимализм
- Цвета модулей: finance=#34C759, tasks=#007AFF, food=#FF9500, sport=#FF3B30, invest=#5856D6
- Навигация: TabBar (5 табов), центр + кнопка Quick Add
- Компоненты: карточки с скруглением 14px, тени 0 1px 4px rgba(0,0,0,0.06)
